`mdvl-ncpamixer` package for `ncpamixer` from URL
<https://github.com/fulhax/ncpamixer>.

Build package by invoking `ant package`.

Recommended alternative package which also allows switching the input sources
and output devices: `pamix` (Debian package `pamix`).
